// Static export: middleware is not supported on GitHub Pages.
// Auth protection is handled client-side via AuthGuard components.
export function middleware() {}
export const config = { matcher: [] };
