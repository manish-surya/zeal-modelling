// Server-side Supabase client (unused in static/GitHub Pages export).
// Kept as a stub so TypeScript doesn't complain about missing module references.
// All data fetching uses the browser client (client.ts) in this deployment.
export {};
